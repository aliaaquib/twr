import AdminPanel from "@/components/AdminPanel";

export const metadata = {
  title: "Admin | The Weekly Roundup",
  description: "Admin dashboard for managing weekly editorial posts.",
};

export default function AdminPage() {
  return <AdminPanel />;
}
